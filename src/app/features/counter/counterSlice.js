import { createSlice, nanoid } from "@reduxjs/toolkit";

const initialState = {
    counters: [],
};

export const counterSlice = createSlice ({
    name: 'counter',
    initialState,
    reducers: {
        
    }
})